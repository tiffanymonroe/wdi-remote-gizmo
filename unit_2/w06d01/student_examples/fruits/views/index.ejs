<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Fruits App</title>
  </head>
  <body>
    <h1> Fruits Index Page</h1>
    <h2>we love fruits</h2>
    <table>
    <% for(let i = 0; i < fruits.length; i++){ %>
      <tr>
          <td>The <a href="/fruits/<%=fruits[i].id%>">
          <%=fruits[i].name; %>
          </a>
           is  <%=fruits[i].color; %>.</td>
          <td><% if(fruits[i].readyToEat === true){ %>
              It is ready to eat!
          <% } else { %>
              It is not ready to eat.
          <% } %></td>
          </tr>
          <% } %>
        </table>
        <br>
        <br>
        <nav>
    <a href="/fruits/new">Create a New Fruit</a>
</nav>
  </body>
</html>
